 
 # 패키지 - 모듈의 계층적 구조(디렉터리 개념)
 # 각 디렉터리는 __init__.py 파일 포함
 
 # 1. 디렉터리 생성
 # 2. 디렉터리 이동
 # 3. 모듈 파일을 디렉터리 복사
 # 4. version=x.0의 __init__.py 생성
 # 5. 서브 디렉터리 생성
 
import packagename.modulename

t = packagename.modulename.add_txt('text', 'python')
 
 