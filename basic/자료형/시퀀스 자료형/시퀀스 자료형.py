
# 시퀀스 자료형

# 문자열, 리스트, 튜플
# 객체가 순서를 가지고 나열

# 인덱싱 - 0부터 값에 접근
# 슬라이싱 - 특정 구간의 값 접근
# 연결 - +연산자로 연결하여 새 시퀀스 자료 생성
# 반복 - *연산자로 n번 반복하여 새 시퀀스 자료 생성
# 맴버체크 - in연산자로 지정 값이 시퀀스 자료의 요소인지 검사
# 크기정보 - len()키워드로 자료의 크기 확인 - 문자열은 문자의 개수 리스트와 튜플은 맴버의 개수

s = '12345'
l = [1,2,3,4,5]
t = (1,2,3,4,5)
